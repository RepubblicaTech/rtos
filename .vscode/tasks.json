{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Kernel",
            "type": "shell",
            "command": "make all && qemu-system-x86_64 -S -gdb tcp::1234 -debugcon file:qemu_gdb.log -m 64M -cdrom kernel-v0.iso -daemonize -smp 2",
        },
        {
            "label": "Build Kernel (UEFI)",
            "type": "shell",
            "command": "make all && qemu-system-x86_64 -S -gdb tcp::1234 -debugcon file:qemu_gdb.log -m 2G -cdrom kernel-v0.iso -daemonize -bios /usr/share/OVMF/OVMF_CODE.fd",
        }
    ]
}